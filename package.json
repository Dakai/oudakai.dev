{
  "name": "oudakai.dev",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "docker:build": "bash scripts/build-and-push.sh",
    "docker:deploy": "bash scripts/redeploy.sh",
    "docker:ship": "bun run docker:build && bun run docker:deploy"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
